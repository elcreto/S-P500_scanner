# S&P 500 Scanner v3.5 — MACD/MACD-V Toggle

Run with:
```
pip install -r requirements.txt
streamlit run streamlit_app.py
```
## Headless export (server)

To produce the ranked table for automation:

```bash
python run_scan_cli.py
